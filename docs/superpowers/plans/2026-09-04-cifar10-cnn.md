# CIFAR-10 CNN Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建结构清晰的 CIFAR-10 简单 CNN 代码基线。

**Architecture:** 数据、模型、训练循环和 checkpoint 独立成模块，由训练和评估入口编排。

**Tech Stack:** Python 3.12、PyTorch、torchvision、pytest

**Spec:** `docs/superpowers/specs/2026-09-04-cifar10-cnn-design.md`

## Global Constraints

- 使用 `training-pipeline/venv`，并在执行 Python、pip 或测试命令前运行 `source training-pipeline/venv/bin/activate`。
- 本轮不安装依赖、不下载数据、不执行训练。
- 不加入 ResNet、云训练或实验追踪。

---

### Task 1: 模型与训练引擎

- [x] 编写模型输出和指标测试。
- [x] 实现 `SimpleCNN`、`train_one_epoch` 和 `evaluate`。
- [ ] 安装依赖后运行测试。

### Task 2: 数据与 checkpoint

- [x] 编写变换和 checkpoint 测试。
- [x] 实现数据加载及 checkpoint 持久化。
- [ ] 安装依赖后运行测试。

### Task 3: 入口与文档

- [x] 编写训练、评估入口和依赖清单。
- [x] 记录代码结构与阅读顺序。
- [x] 执行 Python 语法检查。
