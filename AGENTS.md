# Repository Agent Instructions

## 飞书操作身份与权限

- 在本仓库相关任务中，所有飞书行为必须统一以 Bot 身份执行，并在调用时显式指定 `--as bot`；不得自动切换或回退为用户身份。
- 如果 Bot 无法访问用户已有资源，必须明确告知用户并请求其向 Bot 共享资源或完成必要授权，不得改用用户身份绕过权限边界。
- Bot 创建任何飞书资源后，包括文件夹、云文档、电子表格、多维表格、幻灯片、Markdown 文件等，都必须立即为当前请求用户授予 `full_access`（完全权限/可管理权限）。
- 创建后必须读回并检查用户权限是否已成功生效；只有资源创建成功且当前请求用户已获得 `full_access`，任务才算完成。
- 如果无法识别当前请求用户、缺少授权能力或授权失败，必须明确告知用户并请求其提供所需信息或完成授权，不得默认为已完成。
- 不得降低、移除或覆盖用户已有的更高权限，除非用户明确要求。

## Python 环境

本仓库中的每个子项目统一使用该子项目目录下的 `venv`，Python 版本为 3.12。不同子项目的虚拟环境相互独立。

执行 Python、pip、测试或训练命令前，先激活对应子项目的虚拟环境：
```bash
source <子项目目录>/venv/bin/activate
```

例如，操作 `training-pipeline` 子项目前运行：
```bash
source training-pipeline/venv/bin/activate
```

## 参考资料

- 默认不读取 `docs/notes/` 下的内容；仅在用户明确要求以该目录为基础开展任务时使用。
